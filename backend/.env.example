DATABASE_URL="postgres://<username>:<password>@<host>:<port>/<dbname>?sslmode=require"
JWT_SECRET="your_jwt_secret_here"
PORT=5000

# Note: Do NOT commit your real .env. Replace placeholders with real values in your deployment platform (Render/Vercel).
